/*  3가지 구조
    순차구조 : 위 -> 아래로
    분기구조 : if switch
    반복구조 : for, while */

let kor = 90; //왼쪽에 있는 값을 오른쪽에 넣음
let math = 80;
let sum = kor + math;


let a = 100;
a = 1;

/*
    변수는 단 하나의 값만 저장할 수 있음
    변수는 메모리 블럭, 공간 (값을 저장하기 위한 공간), 새로 쓰기
*/

/*
a? 1 (O) : 새로 쓰기
*/

alert(a);